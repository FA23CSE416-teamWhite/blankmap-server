module.exports = {
    testEnvironment: 'node', // Set the test environment (use 'jsdom' for browser-like environment)
    testMatch: ['**/__tests__/**/*.js', '**/?(*.)+(spec|test).js'], // Specify the test file patterns
  };